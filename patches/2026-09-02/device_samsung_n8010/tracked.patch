diff --git a/BoardConfig.mk b/BoardConfig.mk
index 9179e15..d96f9f0 100644
--- a/BoardConfig.mk
+++ b/BoardConfig.mk
@@ -17,8 +17,11 @@
 # Include n80xx BoardConfigCommon
 -include device/samsung/n80xx-common/BoardConfigCommon.mk
 
+# n8010 is a non-Treble device: override smdk4412-common's VINTF enforcement
+PRODUCT_ENFORCE_VINTF_MANIFEST_OVERRIDE := false
+
 # Inline kernel building
-TARGET_KERNEL_CONFIG := lineageos_n8000_defconfig
+TARGET_KERNEL_CONFIG := lineageos_n8010_defconfig
 
 N8010 := true
 
@@ -35,3 +38,10 @@ SYSTEM_EXT_PRIVATE_SEPOLICY_DIRS += device/samsung/n80xx-common/selinux/private
 # Temporarily set SELinux to permissive for debugging binder issues
 # TODO: Create proper SELinux policies and remove this
 BOARD_KERNEL_CMDLINE += androidboot.selinux=permissive
+# Diagnose (Build 31): surfaceflinger stirbt mit SIGSEGV, aber crash_dump32
+# stuerzt beim Dump selbst ab ("Untracked pid ... received signal 11"), daher
+# kein Tombstone. Mit CONFIG_DEBUG_USER=y schreibt der Kernel bei user_debug=31
+# fuer jeden User-Fault pc/lr/sp/r0-r12 und die Fault-Adresse nach dmesg.
+# (audit=0 aus Build 28-30 war wirkungslos: die avc-Flut kam nicht vom Kernel,
+# sondern von logd via /dev/kmsg -- jetzt per ro.logd.auditd.dmesg=false aus.)
+BOARD_KERNEL_CMDLINE += user_debug=31
diff --git a/docker-build.sh b/docker-build.sh
index 503773b..6c813b1 100755
--- a/docker-build.sh
+++ b/docker-build.sh
@@ -82,9 +82,10 @@ cmd_init() {
 # Sync source
 cmd_sync() {
     log_info "Syncing LineageOS source (this will take a while)..."
+    log_warn "Using -j1 to avoid repository corruption and rate limits"
     docker exec -u build $CONTAINER_NAME bash -c "
         cd /lineage/src && \
-        repo sync -c -j\$(nproc) --force-sync --no-clone-bundle --no-tags
+        repo sync -c -j1 --force-sync --no-clone-bundle --no-tags
     "
 }
 
@@ -93,6 +94,7 @@ cmd_breakfast() {
     log_info "Setting up device tree for $DEVICE..."
     docker exec -u build $CONTAINER_NAME bash -c "
         cd /lineage/src && \
+        mkdir -p device/samsung && \
         ln -sf /lineage/device/samsung/android_device_samsung_n8010 device/samsung/n8010 && \
         source build/envsetup.sh && \
         breakfast $DEVICE
diff --git a/n8010.mk b/n8010.mk
index 35560fc..e296b79 100644
--- a/n8010.mk
+++ b/n8010.mk
@@ -24,12 +24,19 @@ PRODUCT_ENFORCE_VINTF_MANIFEST := false
 
 # Rootdir
 PRODUCT_COPY_FILES += \
-    $(LOCAL_PATH)/rootdir/init.target.rc:$(TARGET_COPY_OUT_VENDOR)/etc/init/hw/init.target.rc
+    $(LOCAL_PATH)/rootdir/init.target.rc:$(TARGET_COPY_OUT_VENDOR)/etc/init/hw/init.target.rc \
+    $(LOCAL_PATH)/rootdir/sf_maps_snapshot.sh:$(TARGET_COPY_OUT_VENDOR)/bin/sf_maps_snapshot.sh
 
 # HIDL
 DEVICE_MANIFEST_FILE := $(LOCAL_PATH)/manifest.xml
 
 $(call inherit-product-if-exists, vendor/samsung/n8010/n8010-vendor.mk)
 
+# Software KeyMint service (AIDL KeyMint 3.0, software-only)
+# Required for Android 14: odsign creates keys with TAG_MAX_BOOT_LEVEL=30 which
+# Samsung's legacy Keymaster 3.0 HIDL HAL does not support. Without this service,
+# odsign silently crashes and wait_for_prop odsign.key.done 1 blocks forever.
+PRODUCT_PACKAGES += android.hardware.security.keymint-service
+
 # Vendor properties
 -include $(LOCAL_PATH)/vendor_prop.mk
\ No newline at end of file
diff --git a/rootdir/init.target.rc b/rootdir/init.target.rc
index 1364a5c..60e67ef 100644
--- a/rootdir/init.target.rc
+++ b/rootdir/init.target.rc
@@ -2,7 +2,7 @@ import /vendor/etc/init/hw/init.target.usb.rc
 import /vendor/etc/init/hw/init.n80xx.rc
 
 on init
-    export LD_SHIM_LIBS /vendor/lib/libsec-ril.so|libsamsung_symbols.so
+    #export LD_SHIM_LIBS /vendor/lib/libsec-ril.so|libsamsung_symbols.so
 
     chown system system /sys/devices/system/cpu/cpufreq/pegasusq
     chown system system /sys/devices/system/cpu/cpufreq/pegasusq/boost_freq
@@ -118,6 +118,11 @@ on boot
 # cbd
     setprop ro.radio.noril yes
 
+# Debug (Build 31): maps des laufenden surfaceflinger sichern, siehe
+# sf_maps_snapshot.sh. Feuert bei jedem (Neu-)Start des Dienstes.
+on property:init.svc.surfaceflinger=running
+    exec_background - root root -- /vendor/bin/sf_maps_snapshot.sh
+
 # serial keyboard daemon
 service vendor.sec_keyboard /vendor/bin/sec_keyboard /dev/ttySAC2
 	class core
diff --git a/system.prop b/system.prop
index cb129dd..f3b79b2 100644
--- a/system.prop
+++ b/system.prop
@@ -3,7 +3,6 @@
 #
 
 dalvik.vm.dexopt-data-only=1
-dalvik.vm.dex2oat-Xmx=256m
 ro.hdcp2.rx=tz
 ro.lcd_min_brightness=20
 ro.sf.lcd_density=160
@@ -48,9 +47,6 @@ ro.HOME_APP_ADJ=1
 # Force high-end graphics in low ram mode
 persist.sys.force_highendgfx=true
 
-# Reduce background apps limit to 16 on low-tier devices
-ro.sys.fw.bg_apps_limit=16
-
 # Set max background services
 ro.config.max_starting_bg=4
 
diff --git a/vendor_prop.mk b/vendor_prop.mk
index fc89745..0740b42 100644
--- a/vendor_prop.mk
+++ b/vendor_prop.mk
@@ -70,10 +70,6 @@ PRODUCT_PROPERTY_OVERRIDES += \
 PRODUCT_PROPERTY_OVERRIDES += \
 	persist.sys.force_highendgfx=true
 
-# Reduce background apps limit to 16 on low-tier devices
-PRODUCT_PROPERTY_OVERRIDES += \
-	ro.sys.fw.bg_apps_limit=16
-
 # Set max background services
 # PRODUCT_PROPERTY_OVERRIDES += \
 	ro.config.max_starting_bg=4
@@ -84,4 +80,15 @@ PRODUCT_PROPERTY_OVERRIDES += \
 
 # Wifi
 #PRODUCT_PROPERTY_OVERRIDES += \
-	wifi.supplicant_scan_interval=240
\ No newline at end of file
+	wifi.supplicant_scan_interval=240
+# Debug-Instrumentierung fuer den surfaceflinger-Absturz (Build 31)
+# - logd schreibt avc-Meldungen sonst selbst nach /dev/kmsg (LogAudit.cpp,
+#   Praefix <38>). flags_health_check erzeugt ~100 davon pro Sekunde und
+#   ueberschreibt den 1-MB-Kernelpuffer in ~15 s. Der Kernel-Parameter audit=0
+#   greift dagegen nicht.
+# - logcatd persistiert alle Puffer (main/system/crash/kernel) nach
+#   /data/misc/logd/logcat*, 32 x 2 MB.
+PRODUCT_PROPERTY_OVERRIDES += \
+	ro.logd.auditd.dmesg=false \
+	persist.logd.logpersistd=logcatd \
+	persist.logd.logpersistd.size=32
